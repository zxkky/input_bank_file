<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Bank File Management</title>
        <!-- Link to Tailwind CSS CDN -->
        <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    </head>
    <body class="bg-gradient-to-b from-blue-500 to-white flex flex-col items-center justify-center min-h-screen p-6">
        <div class="container mx-auto p-4 bg-white m-6 shadow-md rounded-lg w-full max-w-lg">
            <h1 class="text-3xl font-bold mb-6 text-center">Profile Photo</h1>
            <!-- Click Photo to Navigate to Profile Page -->
            <div class="flex justify-center mb-4">
                <a href="/profile">
                    <img src="<%= userProfilePhoto %>" alt="Profile Photo" class="w-40 h-40 rounded-full cursor-pointer hover:opacity-80 transition-opacity">
                </a>
            </div>
        
            <!-- Description -->
            <p class="text-center text-lg font-medium text-gray-700">
                Click the photo to go to the profile page.
            </p>
        </div>

        <div class="container mx-auto p-4 bg-white shadow-md rounded-lg w-full max-w-lg">
            <h1 class="text-3xl font-bold mb-6 text-center">Bank File Management</h1>
            
            <div class="space-y-4">
                <!-- Everyone Can View Bank Files -->
                <a href="/file" class="block text-center px-4 py-2 bg-blue-500 text-white font-bold rounded-md shadow-sm hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                    View All Bank Files
                </a>

                <!-- Only Admins Can Add New Bank Files -->
                <% if (userRole === 'Admin' || userRole === 'Super Admin') { %>
                    <a href="/upload" class="block text-center px-4 py-2 bg-green-500 text-white font-bold rounded-md shadow-sm hover:bg-green-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500">
                        Add New Bank File
                    </a>
                    
                    <!-- Button to Navigate to Admin Page -->
                    <a href="/admin" class="block text-center px-4 py-2 bg-purple-500 text-white font-bold rounded-md shadow-sm hover:bg-purple-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500">
                        Admin
                    </a>
                    <a href="/add-borrower" class="block text-center px-4 py-2 bg-pink-500 text-white font-bold rounded-md shadow-sm hover:bg-purple-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500">
                        Add Borrower
                    </a>
                <% } %>
            </div>
        </div>

        <!-- Logout Button -->
        <div class="container mx-auto p-4 bg-white shadow-md rounded-lg w-full max-w-lg mt-6">
            <form action="/logout" method="POST">
                <button type="submit" class="w-full text-center px-4 py-2 bg-red-500 text-white font-bold rounded-md shadow-sm hover:bg-red-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
                    Logout
                </button>
            </form>
        </div>
    </body>
</html>
